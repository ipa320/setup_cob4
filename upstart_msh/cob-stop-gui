#!/bin/bash
echo "------------------------------------------------"
echo "Stopping the Windows VM...."
su mimic -c "ssh -o ConnectTimeout=15 myrobotname-h1 'export DISPLAY=:0 && vboxmanage controlvm myrobotname-win acpipowerbutton'"
echo "------------------------------------------------"
